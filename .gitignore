node_modules/

# Supabase CLI: generados en local, no commitear
supabase/.temp/

# No incluir en deployments (solo uso interno)
PLAN_MASTER.md

# Valores reales solo en local (generar remote.js con: node scripts/generate-remote.js)
remote.local.js
